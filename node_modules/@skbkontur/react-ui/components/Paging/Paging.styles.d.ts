import { ITheme } from '../../lib/theming/Theme';
declare const jsStyles: {
    dots(t: ITheme): string;
    forwardLink(t: ITheme): string;
    disabled(t: ITheme): string;
    pageLink(t: ITheme): string;
    active(t: ITheme): string;
    focused(t: ITheme): string;
    pageLinkHint(t: ITheme): string;
};
export default jsStyles;
