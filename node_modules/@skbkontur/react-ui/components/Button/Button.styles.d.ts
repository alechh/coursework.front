import { ITheme } from '../../lib/theming/Theme';
declare const jsClasses: {
    root(t: ITheme): string;
    warning(t: ITheme): string;
    error(t: ITheme): string;
    sizeSmall(t: ITheme): string;
    sizeMedium(t: ITheme): string;
    sizeLarge(t: ITheme): string;
    link(t: ITheme): string;
    focus(t: ITheme): string;
    disabled(t: ITheme): string;
    arrow_warning(t: ITheme): string;
    arrow_error(t: ITheme): string;
    default(t: ITheme): string;
    primary(t: ITheme): string;
    success(t: ITheme): string;
    danger(t: ITheme): string;
    pay(t: ITheme): string;
    checked(t: ITheme): string;
    wrap(t: ITheme): string;
};
export default jsClasses;
