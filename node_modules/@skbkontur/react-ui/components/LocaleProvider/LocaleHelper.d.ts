import { LangCodes } from './types';
export declare class LocaleHelper<C> {
    private readonly locales;
    private readonly defaultLangCode;
    constructor(locales: {
        [key in LangCodes]?: C;
    }, langCode?: LangCodes);
    get(langCode?: LangCodes): C;
}
