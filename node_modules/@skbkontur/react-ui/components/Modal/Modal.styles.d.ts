import { ITheme } from '../../lib/theming/Theme';
declare const jsStyles: {
    bg(t: ITheme): string;
    window(t: ITheme): string;
    centerContainer(t: ITheme): string;
    close(t: ITheme): string;
    closeWrapper(): string;
    footer(t: ITheme): string;
    fixedHeader(t: ITheme): string;
    fixedFooter(t: ITheme): string;
};
export default jsStyles;
