import * as React from 'react';
export interface RemoveIconProps {
    className?: string;
    onClick?: React.MouseEventHandler<SVGElement>;
}
declare const TokenRemoveIcon: (props: RemoveIconProps) => JSX.Element;
export default TokenRemoveIcon;
