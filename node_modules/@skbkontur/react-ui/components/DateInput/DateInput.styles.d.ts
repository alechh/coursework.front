import { ITheme } from '../../lib/theming/Theme';
declare const jsStyles: {
    icon(t: ITheme): string;
    iconSmall(t: ITheme): string;
    iconMedium(t: ITheme): string;
    iconLarge(t: ITheme): string;
    iconDisabled(t: ITheme): string;
};
export default jsStyles;
