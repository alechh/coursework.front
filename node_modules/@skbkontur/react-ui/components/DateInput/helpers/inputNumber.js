"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var constants_1 = require("../../../lib/date/constants");
var types_1 = require("../../../lib/date/types");
exports.inputNumber = function (type, prev, key, inputMode, cb) {
  var nextInputMode = false;
  var next;
  var first = 10;
  var length = constants_1.LENGTH_YEAR;
  if (type === types_1.InternalDateComponentType.Month) {
    first = 1;
    length = constants_1.LENGTH_MONTH;
  }
  if (type === types_1.InternalDateComponentType.Date) {
    first = 3;
    length = constants_1.LENGTH_DATE;
  }
  if (!inputMode) {
    next = key;
    nextInputMode = Number(key) <= first;
  } else
  {
    next = ("" + (prev === null ? '' : prev) + key).slice(-length);
    nextInputMode = next.length < length;
  }
  cb(next, nextInputMode);
};