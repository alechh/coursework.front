import { InternalDateComponentType } from '../../../lib/date/types';
export declare const inputNumber: (type: InternalDateComponentType | null, prev: string | number | null, key: string, inputMode: boolean, cb: (next: number | null, inputMode: boolean) => void) => void;
