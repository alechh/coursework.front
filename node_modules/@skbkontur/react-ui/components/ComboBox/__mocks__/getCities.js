"use strict";exports.__esModule = true;exports["default"] = void 0;var kladr = require('./kladr.json');

var delay = function delay(ms) {return function (v) {return new Promise(function (resolve) {return setTimeout(resolve, ms, v);});};};

var citiesFilterPredicate = function citiesFilterPredicate(query) {return function (city) {return (
      city.Id.toString() === query || city.City.toLowerCase().includes(query.toLowerCase()));};};

var maxItems = 5;

var getCities = function getCities(query) {
  var items = kladr.filter(citiesFilterPredicate(query));
  var result = items.slice(0, maxItems);
  return Promise.resolve({
    foundItems: result,
    totalCount: items.length }).
  then(delay(300));
};var _default =

getCities;exports["default"] = _default;