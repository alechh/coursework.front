import * as React from 'react';
import Input, { InputProps } from '../Input';
export declare type PasswordInputFallbackProps = {
    visible?: boolean;
    size?: string;
    refInput: (element: Input) => void;
} & InputProps;
export default class PasswordInputFallback extends React.Component<PasswordInputFallbackProps> {
    static defaultProps: {
        size: string;
    };
    private _passwordInput;
    private _textInput;
    componentDidMount(): void;
    componentDidUpdate(prevProps: PasswordInputFallbackProps): void;
    render(): JSX.Element;
    private _handleFocus;
}
