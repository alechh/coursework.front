import { APIProvider, AddressResponse, SearchOptions, VerifyResponse, APIResult, FiasCountry } from '../types';
import { Address } from '../models/Address';
export declare class MockAPI implements APIProvider {
    verify: (address: Address) => Promise<APIResult<VerifyResponse>>;
    search: ({ fiasId, searchText, field, parentFiasId, limit, fullAddress, }: SearchOptions) => Promise<APIResult<AddressResponse[]>>;
    searchCountry: (options: SearchOptions) => Promise<APIResult<FiasCountry[]>>;
    private getAddresses;
}
export default MockAPI;
