import * as React from 'react';
import { ComboBoxProps } from '../../ComboBox';
import { Address } from '../models/Address';
export interface FiasComboBoxProps extends ComboBoxProps<Address> {
    limit?: number;
}
export interface FiasComboBoxChangeEvent {
    target: {
        value: Address;
    };
}
interface FiasComboBoxState {
    searchText: string;
    totalCount: number;
}
export declare class FiasComboBox extends React.Component<FiasComboBoxProps, FiasComboBoxState> {
    static defaultProps: {
        itemToValue: (item: import("../../ComboBox").ComboBoxItem) => string;
        valueToString: (item: import("../../ComboBox").ComboBoxItem) => string;
        renderValue: (item: import("../../ComboBox").ComboBoxItem) => string;
        renderItem: (item: import("../../ComboBox").ComboBoxItem) => string;
        menuAlign: string;
        searchOnFocus: boolean;
        drawArrow: boolean;
    };
    state: FiasComboBoxState;
    private combobox;
    readonly hasItems: boolean;
    render(): JSX.Element;
    private createRef;
    private getItems;
    private renderItem;
    private renderTotalCount;
    private handleChange;
    private handleInputChange;
    private handleFocus;
    private highlight;
}
export default FiasComboBox;
