import { ITheme } from '../../lib/theming/Theme';
declare const jsStyles: {
    root(t: ITheme): string;
    hover(t: ITheme): string;
    selected(t: ITheme): string;
    disabled(t: ITheme): string;
    link(t: ITheme): string;
    withIcon(t: ITheme): string;
};
export default jsStyles;
