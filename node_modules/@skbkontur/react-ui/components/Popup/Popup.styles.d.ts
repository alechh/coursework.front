import { ITheme } from '../../lib/theming/Theme';
declare const jsStyles: {
    popup(t: ITheme): string;
    content(t: ITheme): string;
    contentInner(t: ITheme): string;
    shadow(t: ITheme): string;
};
export default jsStyles;
