import { ITheme } from '../../lib/theming/Theme';
declare const jsStyles: {
    root(t: ITheme): string;
    arrow(t: ITheme): string;
    menuHolder(t: ITheme): string;
    menuItem(t: ITheme): string;
    menuItemSelected(t: ITheme): string;
    menuItemActive(t: ITheme): string;
    menuItemDisabled(t: ITheme): string;
    menuUp(t: ITheme): string;
    menuDown(t: ITheme): string;
};
export default jsStyles;
