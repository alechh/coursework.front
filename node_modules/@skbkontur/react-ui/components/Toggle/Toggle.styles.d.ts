import { ITheme } from '../../lib/theming/Theme';
declare const jsStyles: {
    handle(t: ITheme): string;
    container(t: ITheme): string;
    focused(t: ITheme): string;
    activeBackground(t: ITheme): string;
    isLoading(t: ITheme): string;
    isWarning(t: ITheme): string;
    isError(t: ITheme): string;
    wrapper(t: ITheme): string;
};
export default jsStyles;
