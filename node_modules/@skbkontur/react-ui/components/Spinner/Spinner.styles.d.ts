import { ITheme } from '../../lib/theming/Theme';
declare const jsStyles: {
    cloudBg(t: ITheme): string;
    cloudStroke(t: ITheme): string;
    cloudStrokeDimmed(t: ITheme): string;
    circle(t: ITheme): string;
    circleStroke(t: ITheme): string;
    circleStrokeDimmed(t: ITheme): string;
    caption(t: ITheme): string;
};
export default jsStyles;
