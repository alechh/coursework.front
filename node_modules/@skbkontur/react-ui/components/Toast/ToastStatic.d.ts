import { Action } from './Toast';
declare class ToastStatic {
    static push: (notification: string, action?: Action | undefined) => void;
    static _push: (notification: string, action?: Action | undefined) => void;
    static close: () => void;
    private static node;
    private static instance;
}
export default ToastStatic;
