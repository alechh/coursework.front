import * as React from 'react';
import { TabsContextType } from './TabsContext';
export interface IndicatorProps {
    className?: string;
    tabUpdates: {
        on: (x0: () => void) => () => void;
    };
    vertical: boolean;
    context?: TabsContextType;
}
export interface IndicatorState {
    styles: React.CSSProperties;
}
export declare class Indicator extends React.Component<IndicatorProps, IndicatorState> {
    state: IndicatorState;
    private theme;
    private eventListener;
    private removeTabUpdatesListener;
    componentDidMount(): void;
    componentWillUnmount(): void;
    componentDidUpdate(_: IndicatorProps, prevState: IndicatorState): void;
    render(): JSX.Element;
    private renderMain;
    private reflow;
    private throttledReflow;
    private getStyles;
}
export declare const IndicatorWithContext: (props: IndicatorProps) => JSX.Element;
export default IndicatorWithContext;
