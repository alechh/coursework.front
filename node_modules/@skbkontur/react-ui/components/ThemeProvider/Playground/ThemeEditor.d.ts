import * as React from 'react';
import { ITheme } from '../../../lib/theming/Theme';
import { PlaygroundTheme, ThemeErrorsType } from './ThemeProviderPlayground';
interface IThemeEditorProps {
    editingTheme: ITheme;
    currentTheme: PlaygroundTheme;
    currentErrors: ThemeErrorsType;
    onValueChange: (variable: keyof PlaygroundTheme, value: string) => void;
}
interface IThemeEditorState {
    groups: IGroup[];
    isLoading: boolean;
}
interface IGroup {
    title: string;
    prefix: string;
    isCommon?: boolean;
}
export declare class ThemeEditor extends React.Component<IThemeEditorProps, IThemeEditorState> {
    state: {
        groups: never[];
        isLoading: boolean;
    };
    private updateTimeout?;
    render(): JSX.Element;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private renderGroups;
}
export {};
