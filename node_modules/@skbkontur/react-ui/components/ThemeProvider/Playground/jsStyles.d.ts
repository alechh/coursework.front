import { PlaygroundTheme } from './ThemeProviderPlayground';
declare const jsStyles: {
    stickyTabsWrapper(t: PlaygroundTheme): string;
    tabsInnerWrapper(t: PlaygroundTheme): string;
    playgroundWrapper(t: PlaygroundTheme): string;
    editorHeaderWrapper(t: PlaygroundTheme): string;
};
export default jsStyles;
