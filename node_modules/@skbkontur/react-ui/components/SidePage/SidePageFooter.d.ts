import * as React from 'react';
import { SidePageContextType } from './SidePageContext';
export interface SidePageFooterProps {
    children?: React.ReactNode | ((fixed: boolean) => React.ReactNode);
    context?: SidePageContextType;
    /**
     * Включает серый цвет в футере
     */
    panel?: boolean;
}
/**
 * Футер модального окна.
 */
export declare class SidePageFooter extends React.Component<SidePageFooterProps> {
    static __KONTUR_REACT_UI__: string;
    state: {
        fixed: boolean;
    };
    private theme;
    private content;
    private wrapper;
    private layoutSub;
    componentDidMount(): void;
    componentWillUnmount(): void;
    render(): JSX.Element;
    update: () => void;
    private renderMain;
    private refContent;
    private refWrapper;
    private setProperStyles;
    private getContentHeight;
}
export declare const SidePageFooterWithContext: (props: SidePageFooterProps) => JSX.Element;
export default SidePageFooterWithContext;
