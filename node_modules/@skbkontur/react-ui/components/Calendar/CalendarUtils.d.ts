import { MonthViewModel } from './MonthViewModel';
import { CalendarState, CalendarProps } from './Calendar';
export declare const calculateScrollPosition: (months: MonthViewModel[], scrollPosition: number, deltaY: number) => {
    scrollPosition: number;
    months: MonthViewModel[];
    scrollDirection: number;
};
export declare const applyDelta: (deltaY: number) => ({ scrollPosition, months }: Readonly<CalendarState>, { minDate, maxDate }: CalendarProps) => {
    scrollPosition: number;
    months: MonthViewModel[];
    scrollDirection: number;
} | {
    scrollPosition: number;
    scrollDirection: number;
};
export declare const isMonthVisible: (top: number, month: MonthViewModel) => boolean;
export declare const getMonthsHeight: (months: MonthViewModel[]) => number;
export declare const getMonths: (month: number, year: number) => MonthViewModel[];
