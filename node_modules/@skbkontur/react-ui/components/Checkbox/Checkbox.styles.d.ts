import { ITheme } from '../../lib/theming/Theme';
declare const jsStyles: {
    root(t: ITheme): string;
    rootWrapperIE11(): string;
    box(t: ITheme): string;
    input(t: ITheme): string;
    warning(t: ITheme): string;
    error(t: ITheme): string;
    checked(t: ITheme): string;
    indeterminate(t: ITheme): string;
    focus(t: ITheme): string;
    disabled(t: ITheme): string;
    iconFixBaseline(): string;
    iconUnchecked(): string;
    caption(t: ITheme): string;
    captionIE11(): string;
};
export declare const classes: {
    root: string;
    box: string;
    disabled: string;
};
export default jsStyles;
