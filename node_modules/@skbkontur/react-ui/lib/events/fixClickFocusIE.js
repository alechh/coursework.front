"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ensureOldIEClassName_1 = require("../../components/ensureOldIEClassName");
var getFocusableElements_1 = require("../dom/getFocusableElements");
exports.fixClickFocusIE = function (e) {
  if (ensureOldIEClassName_1.isIE || ensureOldIEClassName_1.isEdge) {
    // workaround for the IE/Edge focus bug
    // https://developer.microsoft.com/en-us/microsoft-edge/platform/issues/14306303/
    if (document.activeElement !== e.target) {
      if (e.target instanceof HTMLElement) {
        var closestFocusable = getFocusableElements_1.getClosestFocusableElement(e.target);
        if (closestFocusable) {
          closestFocusable.focus();
        }
      }
    }
  }
};