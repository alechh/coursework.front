import { ITheme, IThemeIn } from './Theme';
export default class ThemeFactory {
    static create(theme: IThemeIn): Readonly<ITheme>;
    static isFullTheme(theme: IThemeIn | ITheme): theme is ITheme;
    static getDefaultTheme(): ITheme;
    static overrideDefaultTheme(theme: IThemeIn): void;
    static getKeys(theme: ITheme): string[];
    private static defaultTheme;
    private static constructTheme;
}
