import { ITheme } from './Theme';
export declare const AnimationKeyframes: {
    spinnerCloudOffset(t: ITheme): string;
    spinnerCloudLength(t: ITheme): string;
    spinnerCircleOffset(t: ITheme): string;
    spinnerCircleLength(t: ITheme): string;
    spinnerCircleRotate(t: ITheme): string;
    spinnerColor(t: ITheme): string;
};
